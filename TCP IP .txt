Le client MySQL est intégré au SERVEUR TCP/IP

Le client envoie les requetes MYSQL au serveur TCP/IP
Le client MySQL envoie les requetes a la BDD
la BDD envoie la reponse aux requetes
Le SERVEUR envoie la reponse au client

Requete possible de l'appli android :

Si le joueur tente de se connecter